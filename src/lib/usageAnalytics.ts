import { invoke } from "@tauri-apps/api/core";

export type EvidenceConfidence = "measured" | "estimated" | "inferred" | "unavailable";
export type Metric = { value: number | null; confidence: EvidenceConfidence; source: string; observedAt: number | null; caveat: string | null };
export type ContextPressure = { usedTokens: number | null; limitTokens: number | null; percent: number | null; projectedPercent: number | null; band: "low" | "medium" | "high" | "critical" | "unavailable"; limitSource: string; caveat: string | null };
export type XraySource = { id: string; label: string; tokensSaved: Metric; detail: string; caveat: string | null };
export type XrayEvent = { id: string; occurredAt: number; kind: string; title: string; detail: string; confidence: EvidenceConfidence };
export type XrayAnomaly = { id: string; severity: "info" | "warning" | "critical"; title: string; detail: string };
export interface TokenXraySnapshot { schemaVersion: number; generatedAt: number; sessionId: string | null; agent: string | null; provider: string | null; model: string | null; freshness: "live" | "recent" | "stale" | "unavailable"; metrics: Record<string, Metric>; contextPressure: ContextPressure; sources: XraySource[]; timeline: XrayEvent[]; anomalies: XrayAnomaly[]; }
export type BriefingAgent = { id: string; label: string; requests: number; spentTokens: Metric; savedTokens: Metric; cachedTokens: Metric; estimatedCostUsd: Metric; highestContextPercent: number | null; detail: string | null };
export type AttentionItem = { id: string; severity: "info" | "warning" | "critical"; title: string; detail: string; destination?: string | null };
export type Recommendation = { id: string; title: string; evidence: string; destination?: string | null; priority: number };
export interface DailyUsageBriefing { schemaVersion: number; dayKey: string; timezone: string; generatedAt: number; completeness: "complete" | "partial" | "insufficient-data"; headline: string | null; totals: Record<string, Metric>; agents: BriefingAgent[]; attentionItems: AttentionItem[]; recommendations: Recommendation[]; evidenceCoverage: { measured: number; estimated: number; inferred: number; unavailable: number; detail?: string | null }; }
export type UsageAnalyticsClearPreview = { briefingCount: number; eventCount: number; detail: string; };

const metric = (raw: unknown): Metric => {
  const value = typeof raw === "number" ? raw : typeof (raw as any)?.value === "number" ? (raw as any).value : null;
  const confidence = (raw as any)?.confidence;
  return { value, confidence: ["measured", "estimated", "inferred", "unavailable"].includes(confidence) ? confidence : value === null ? "unavailable" : "inferred", source: (raw as any)?.source ?? "Local analytics", observedAt: (raw as any)?.observedAt ?? (raw as any)?.observed_at ?? null, caveat: (raw as any)?.caveat ?? null };
};
const list = <T,>(value: unknown): T[] => Array.isArray(value) ? value as T[] : [];
const record = (value: unknown): Record<string, any> => value && typeof value === "object" ? value as Record<string, any> : {};

export function normalizeXray(raw: unknown): TokenXraySnapshot {
  const data = record(raw); const metricsRaw = record(data.metrics);
  const pressureRaw = record(data.contextPressure ?? data.context_pressure);
  const used = pressureRaw.usedTokens ?? pressureRaw.used_tokens ?? null;
  const limit = pressureRaw.limitTokens ?? pressureRaw.limit_tokens ?? null;
  const percent = pressureRaw.percent ?? (typeof used === "number" && typeof limit === "number" && limit > 0 ? Math.round((used / limit) * 100) : null);
  const rawBand = pressureRaw.band ?? (percent === null ? "unavailable" : percent >= 95 ? "critical" : percent >= 80 ? "high" : percent >= 60 ? "medium" : "low");
  const band = rawBand === "normal" ? "low" : rawBand === "elevated" ? "medium" : rawBand;
  const timestamp = (value: unknown) => typeof value === "number" ? value : typeof value === "string" ? Date.parse(value) || 0 : 0;
  return { schemaVersion: data.schemaVersion ?? data.schema_version ?? 1, generatedAt: timestamp(data.generatedAt ?? data.generated_at), sessionId: data.sessionId ?? data.session_id ?? null, agent: data.agent ?? null, provider: data.provider ?? null, model: data.model ?? null, freshness: data.freshness ?? "unavailable", metrics: Object.fromEntries(["inputTokens", "outputTokens", "cacheReadTokens", "cacheWriteTokens", "savedTokens", "avoidedTokens", "estimatedCostUsd", "estimatedSavingsUsd"].map((key) => [key, metric(metricsRaw[key] ?? metricsRaw[key.replace(/[A-Z]/g, (c) => `_${c.toLowerCase()}`)])])), contextPressure: { usedTokens: typeof used === "number" ? used : null, limitTokens: typeof limit === "number" ? limit : null, percent: typeof percent === "number" ? percent : null, projectedPercent: pressureRaw.projectedPercent ?? pressureRaw.projected_percent ?? null, band, limitSource: pressureRaw.limitSource ?? pressureRaw.limit_source ?? "No credible context limit recorded", caveat: pressureRaw.caveat ?? null }, sources: list<any>(data.sources).map((item) => ({ id: item.id ?? item.label ?? item.source ?? "source", label: item.label ?? item.source ?? "Optimization source", tokensSaved: metric(item.tokensSaved ?? item.tokens_saved), detail: item.detail ?? (list<string>(item.evidence).join(" · ") || "No source detail recorded."), caveat: item.caveat ?? null })), timeline: list<any>(data.timeline).map((item, index) => ({ id: item.id ?? `${item.occurredAt ?? item.occurred_at ?? index}`, occurredAt: timestamp(item.occurredAt ?? item.occurred_at), kind: item.kind ?? "usage", title: item.title ?? item.label ?? item.kind ?? "Usage event", detail: item.detail ?? "", confidence: item.confidence ?? "inferred" })), anomalies: list<any>(data.anomalies).map((item, index) => ({ id: item.id ?? String(index), severity: item.severity ?? "warning", title: item.title ?? item.message ?? "Usage anomaly", detail: item.detail ?? list<string>(item.evidence).join(" · ") })) };
}

export function normalizeBriefing(raw: unknown): DailyUsageBriefing {
  const data = record(raw); const totals = record(data.totals);
  const timestamp = (value: unknown) => typeof value === "number" ? value : typeof value === "string" ? Date.parse(value) || 0 : 0;
  return { schemaVersion: data.schemaVersion ?? data.schema_version ?? 1, dayKey: data.dayKey ?? data.day_key ?? new Date().toISOString().slice(0, 10), timezone: data.timezone ?? Intl.DateTimeFormat().resolvedOptions().timeZone, generatedAt: timestamp(data.generatedAt ?? data.generated_at), completeness: data.completeness ?? "insufficient-data", headline: data.headline ?? null, totals: Object.fromEntries([["requests", totals.requests], ["spentTokens", totals.inputTokens ?? totals.input_tokens], ["savedTokens", totals.savedTokens ?? totals.saved_tokens], ["cachedTokens", totals.cacheReadTokens ?? totals.cache_read_tokens], ["avoidedTokens", totals.avoidedTokens ?? totals.avoided_tokens], ["estimatedCostUsd", totals.estimatedCostUsd ?? totals.estimated_cost_usd], ["estimatedSavingsUsd", totals.estimatedSavingsUsd ?? totals.estimated_savings_usd]].map(([key, value]) => [key, metric(value)])), agents: list<any>(data.agents).map((agent, index) => ({ id: agent.id ?? String(index), label: agent.label ?? agent.agent ?? agent.provider ?? agent.id ?? "Unattributed agent", requests: Number(agent.requests ?? 0), spentTokens: metric(agent.spentTokens ?? agent.spent_tokens ?? agent.inputTokens ?? agent.input_tokens), savedTokens: metric(agent.savedTokens ?? agent.saved_tokens), cachedTokens: metric(agent.cachedTokens ?? agent.cached_tokens), estimatedCostUsd: metric(agent.estimatedCostUsd ?? agent.estimated_cost_usd), highestContextPercent: agent.highestContextPercent ?? agent.highest_context_percent ?? null, detail: agent.detail ?? (agent.failures ? `${agent.failures} failures` : null) })), attentionItems: list<any>(data.attentionItems ?? data.attention_items).map((item, index) => ({ id: item.id ?? String(index), severity: item.severity ?? "warning", title: item.title ?? "Needs attention", detail: item.detail ?? list<string>(item.evidence).join(" · "), destination: item.destination ?? null })), recommendations: list<any>(data.recommendations).slice(0, 3).map((item, index) => ({ id: item.id ?? item.ruleId ?? String(index), title: item.title ?? item.actionLabel ?? item.ruleId ?? "Review analytics", evidence: item.evidence instanceof Array ? item.evidence.join(" · ") : item.evidence ?? item.reason ?? "", destination: item.destination ?? null, priority: item.priority ?? item.priorityScore ?? index })), evidenceCoverage: { measured: Number(data.evidenceCoverage?.measured ?? data.evidenceCoverage?.measuredSources ?? data.evidence_coverage?.measured ?? data.evidence_coverage?.measured_sources ?? 0), estimated: Number(data.evidenceCoverage?.estimated ?? data.evidenceCoverage?.estimatedSources ?? data.evidence_coverage?.estimated ?? data.evidence_coverage?.estimated_sources ?? 0), inferred: Number(data.evidenceCoverage?.inferred ?? data.evidenceCoverage?.inferredSources ?? data.evidence_coverage?.inferred ?? data.evidence_coverage?.inferred_sources ?? 0), unavailable: Number(data.evidenceCoverage?.unavailable ?? data.evidenceCoverage?.unavailableMetrics ?? data.evidence_coverage?.unavailable ?? data.evidence_coverage?.unavailable_metrics ?? 0), detail: data.evidenceCoverage?.detail ?? data.evidenceCoverage?.notes?.join(" · ") ?? data.evidence_coverage?.detail ?? data.evidence_coverage?.notes?.join(" · ") ?? null } };
}

export async function loadTokenXraySnapshot() { return normalizeXray(await invoke("get_token_xray_snapshot")); }
export async function loadDailyUsageBriefing() { return normalizeBriefing(await invoke("get_daily_usage_briefing")); }
export async function loadDailyUsageBriefingHistory() { const result = await invoke<unknown[]>("list_daily_usage_briefings"); return list(result).map(normalizeBriefing); }
export async function previewClearUsageAnalytics(): Promise<UsageAnalyticsClearPreview> { const raw = record(await invoke("preview_clear_usage_analytics")); return { briefingCount: Number(raw.briefingCount ?? raw.briefing_count ?? raw.affectedBriefings ?? 0), eventCount: Number(raw.eventCount ?? raw.event_count ?? raw.affectedEvents ?? 0), detail: raw.detail ?? raw.summary ?? "This only removes local usage analytics. Your savings ledger is not included unless the preview explicitly says so." }; }
export async function clearUsageAnalytics(): Promise<UsageAnalyticsClearPreview> { const raw = record(await invoke("clear_usage_analytics")); return { briefingCount: Number(raw.briefingCount ?? raw.briefing_count ?? raw.deletedBriefings ?? 0), eventCount: Number(raw.eventCount ?? raw.event_count ?? raw.deletedEvents ?? 0), detail: raw.detail ?? raw.summary ?? "Local usage analytics cleared." }; }
export async function exportDailyUsageBriefing(format: "markdown" | "json") { const result = await invoke<{ markdown?: string; json?: string; briefing?: unknown }>("export_daily_usage_briefing"); return format === "markdown" ? result.markdown ?? "" : result.json ?? JSON.stringify(result.briefing ?? {}, null, 2); }
export function metricLabel(metric: Metric) { return metric.confidence; }
export function formatMetric(metric: Metric, currency = false) { if (metric.value === null) return "Unavailable"; return currency ? new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 2 }).format(metric.value) : new Intl.NumberFormat("en-US", { notation: metric.value >= 1000 ? "compact" : "standard", maximumFractionDigits: 1 }).format(metric.value); }

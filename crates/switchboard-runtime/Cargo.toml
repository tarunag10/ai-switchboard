[package]
name = "switchboard-runtime"
version = "0.1.0"
description = "Platform-neutral runtime adapter contracts for AI Switchboard."
license = "MIT"
edition = "2021"

[dependencies]
switchboard-core = { path = "../switchboard-core" }

[package]
name = "switchboard-core"
version = "0.1.0"
description = "Provider-neutral AI Switchboard contracts shared by desktop and CLI surfaces."
license = "MIT"
edition = "2021"

[dependencies]
serde = { version = "=1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "=1.0.149"

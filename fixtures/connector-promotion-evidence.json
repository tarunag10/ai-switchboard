{
  "schemaVersion": 1,
  "requiredSidecarStages": [
    "detected",
    "manualGuide",
    "backupImplemented",
    "applyImplemented",
    "verifyImplemented",
    "rollbackImplemented",
    "offCleanupImplemented"
  ],
  "promotedNativeConnectorIds": ["gemini_cli", "goose", "grok_cli", "opencode", "windsurf", "zed_ai"],
  "gatedNativeConnectorIds": ["cursor"],
  "notes": [
    "Sidecar promotion requires every lifecycle stage to be ready on fixture homes.",
    "Native provider writes remain a separate gate from managed sidecar routing."
  ]
}
